To start on boot using upstart:

sudo cp minvy.conf /etc/init/

To start / stop - use
sudo start minvy
stop minvy

To setup monit for auto restarting the app on failure
chmod +x ./runmonit
sudo cp monitrc /etc/monit/monitrc

To start monitoring
sudo ./runmonit


