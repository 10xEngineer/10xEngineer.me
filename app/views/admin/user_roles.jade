extends ../layout

block layout.body
  .container
    h1 Assign Roles of #{extUser.name}

    p
      form(method="post")
        p
          label(for="roles") Roles

          select#selRole(name="chngRoles", multiple)
            each role in roles
              -if(extUser.roles.join(', ').indexOf(role.name) >= 0)
                option(value="#{role.name}", selected) #{role.name}
              -else
                option(value="#{role.name}") #{role.name}

          div#permission.select2-container.select2-container-multi
            div.select2-drop
              ul.select2-results

        p
          a#cancel.btn.btn-small(href="/admin") Cancel
          &nbsp;
          input.btn.btn-small(type="submit", value="Apply Changes")

    script
      function init() {
        $("#selRole").select2();        
      }
