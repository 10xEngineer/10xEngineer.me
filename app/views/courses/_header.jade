.container-fluid
	.row-fluid
		.span10
			ul.breadcrumb
				li
					a(href='/courses') Courses
					span.divider /
				- if(typeof (course) != 'undefined')
					li
						a(href='/courses/#{course._id}') #{course.title}
						span.divider /
					- if(typeof (unit_id) != 'undefined' && typeof(unit) != 'undefined')
						li
							a(href='/courses/#{course._id}/#{unit_id}') #{unit.title}
							span.divider /
						- if(typeof (lesson_id) != 'undefined' && typeof(lesson) != 'undefined')
							li
								a(href='/courses/#{course._id}/#{unit_id}/#{lesson_id}') #{lesson.title}
								span.divider /

		//
			.span7
				ul.nav.nav-pills
					li.active
						a(href='#overview') Overview
					li
						a(href='/ContentManager') Course Content
					li
						a(href='/') Discussion
					li
						a(href='#announcements') Announcements
					li
						a(href='/progress') Progress