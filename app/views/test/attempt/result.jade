.container
	h1 Congratulations !!!
	hr
	h3 Dear #{userName},
	h4 You successfully completed the test and your result is 	
		- var per = Math.round((assessment.score * 100 / test.mark)*100)/100;
		code(style="padding:8px;") #{per}%  (Marks : #{assessment.score}/#{test.mark})
	hr